# 更新日志 / Changelog

> 中英双语摘要（按开发阶段倒序）。完整逐次改动时间线见 `audit/CHANGELOG.html`（中文）。
> Bilingual summary by development phase (newest first). The full chronological log is in `audit/CHANGELOG.html` (Chinese).

---

## 阶段三十九 / Phase 39 — 三版统一上游对比（Glimpse v1.3.0 / Illustrations v0.5.2）（2026-08-16）
- **中文**：用户核验发现「相比上游的改进」对比段仍引用过时的 Glimpse「最新 v1.2.5」，且插件内「关于 美术馆」文本与独立对比件（`artgallery_vs_glimpse_compare.html`）三方事实不一致。已上 GitHub 实查核实版本：Glimpse 最新稳定版 **v1.3.0**（2026-08-15，非预发布；v1.2.13 等为预发布）、Illustrations 最新仍 **v0.5.2**（2026-04-25，未变）。据此统一三版：① README § 相比上游的改进——引言改"吸收基线"口径、Glimpse 版本注更正为 v1.3.0、表后补「关于 Glimpse v1.3.0」差异说明（书签美术馆独立实现 / 屏上缩放与全局开关未采用 / 无闪烁切图对 e-ink 无收益）；② 独立对比件刷新为覆盖**双母插件**新版（原仅比 Glimpse v1.2.2），并 vendor 进仓库 `audit/` 以便版本受控；③ 插件内「关于 美术馆」文本重写——注明两母插件最新版本与作者、Glimpse v1.3.0 书签独立实现、未采用其屏上缩放控件与全局总开关。仅文档/文案改动，无运行行为变化；LuaJIT 语法 `SYNTAX_OK`；三副本（工作≡干净）一致。未升版本号、未发版（依「发版须经同意」纪律）；将推送 GitHub（main 分支），发行包 zip 内 README/关于文本仍反映 v1.0.20 发版时状态，如需可重新发版。
- **English**: User audit found the "Improvements over the upstreams" section still cited a stale Glimpse "latest v1.2.5", and the in-plugin "About ArtGallery" text and the standalone compare file disagreed. Verified on GitHub: Glimpse latest stable **v1.3.0** (2026-08-15), Illustrations still **v0.5.2**. Unified the three versions: ① README section — baseline wording, Glimpse version corrected to v1.3.0, added a "On Glimpse v1.3.0" divergence note; ② standalone compare file refreshed to cover **both** parents (was Glimpse-only) and vendored into the repo `audit/`; ③ in-plugin About text rewritten with both parents' latest versions, authors, and the v1.3.0 divergence. Docs/text only, no runtime change; syntax OK; working≡clean copies identical. No version bump, no release; to be pushed to GitHub main. The release-zip asset still reflects v1.0.20 wording and can be re-cut on request.

## 阶段三十八 / Phase 38 — 书签入画廊（独立「书签」段 · 可收藏 · 视作缓存）（2026-08-16）
- **中文**：新增「书签入画廊」功能（默认关闭，插件菜单「图库包含书签页」开关；开启后需重新打开美术馆生效，且仅当本书确有书签时才有意义）。把用户在书中手工添加的「狗耳朵」书签页渲染成缩略图，作为图库里**独立的「书签」段**，严格满足此前四点硬性要求：① **书签计数不并入「全部」**——底部分段切换器新增「书签[B]」段、`全部[N]` 仍只含 shown∪ignored；无书签时「书签」段不可见（仿忽略段新增 `_hasBookmarkTab()`）；② **书签永不进「忽略」池**——长按菜单「忽略图片」对书签 meta 置灰（`enabled` 加 `not meta.is_bookmark`），`on_ignore` 回调加 `if meta.is_bookmark then return end` 双保险；③ **书签页可被收藏**——长按菜单「收藏图片」走 `addFavorite` 的 `is_bookmark` 分支：渲染 PNG 存入 `bookmark_cache` 并记为带 `is_bookmark=true` 的收藏记录，全局「收藏」画廊与本书「收藏」段均可见已收藏书签；④ 生成的 PNG **视作缓存**——可被「清除本书图片缓存」(`_clearBookCache`，按本书 `<dochash>_` 前缀删并剔除本书书签收藏记录) 清除，另享 64MB 上限跨书冷文件（mtime）淘汰（`_pruneBookmarkDiskCache`）。渲染委托 KOReader 内置 `ReaderThumbnail` 子进程（`getPageThumbnail`，签名已对源码核实），非自建引擎；磁盘命中（`DataStorage:getDataDir()/artgallery/bookmark_cache/<dochash>_p{page}_w{w}_h{h}{_nm}.png`）即时载入并跳过子进程，未就绪返回白底占位、子进程完成后经 `ArtGalleryViewer:_onBookmarkThumbReady` 重建画廊网格替换占位（含同步路径二次检查，避免占位残留）。同步增强：① `_collectBookmarkMetas` 取 `self.ui.annotation`/`self.ui.bookmark`（跳过高亮 `a.drawer`），meta 含 `is_bookmark=true`、`spine_index=page` 近似阅读顺序、按页码排序；② `_ensureDerived` 把书签渲染闭包并入 `_render_of`，使「收藏」派生列表能显示已收藏书签；③ `_favoritesLists` 扫描 `bookmark_metas`；④ `_cycleGalleryFilter` 循环顺序扩展为 全部→收藏→书签→忽略（仅纳入可见段）；⑤ viewer 关闭时 `onCloseWidget` 调 `_freeBookmarkThumbs` 释放缓存副本并取消在途渲染请求，防内存/句柄泄漏；⑥ 关于文档与设置项同步说明本功能。限制（已修订，见下方「书签可跳转补记」「书签全屏看图+删除补记」）：书签不入图片全屏查看器主池，但交互已完整——**轻点书签缩略图 = 进入单图全屏查看该书签页**（仿母插件 glimpse 风，高清重渲染：先以已渲染缩略图瞬时显示，再异步拉取屏幕分辨率高清渲染替换），**长按书签弹菜单**：「跳转到此页」（一键回该书签页，阅读器全屏呈现）/「收藏图片·取消收藏」（满足要求③、走 addFavorite 的 is_bookmark 分支、非损坏项）/剔除「忽略图片」（满足要求②：书签永不进忽略池）/「删除书签」（双向删除：经 ConfirmBox 二次确认后从 KOReader 书本身移除该狗耳朵书签）。书签段真实意义是「可视化书签目录」：既能全屏看图、也能一键跳转或删除书中书签。校验：LuaJIT 语法 `SYNTAX_OK`；`getBookmarkPageNumber`/`getPageThumbnail` 签名已对 KOReader 源码核实一致。版本号维持 **v1.0.19 未升版**；未推送、未发版（依「发版须经同意」纪律），待 KPW3 实机三验（中文源/切英文全英文/框架串正常）与真机确认书签渲染/收藏/清理/跳转。
- **English**: New "bookmarks into gallery" feature (off by default; plugin-menu toggle "图库包含书签页"; takes effect after reopening ArtGallery, and only meaningful when the book actually has bookmarks). Renders the user's hand-made "dog-ear" bookmark pages into thumbnails as a **separate "书签" (Bookmarks) tab**, strictly meeting the four hard requirements: ① **bookmarks are NOT counted in "全部" (All)** — the bottom segmented switcher gains a "书签[B]" segment while "全部[N]" still only covers shown∪ignored; the Bookmarks segment is hidden when empty (new `_hasBookmarkTab()`, mirroring the Ignored segment); ② **bookmarks never enter the Ignored pool** — the long-press "忽略图片" (Ignore) item is greyed out for bookmark metas (`enabled` gains `not meta.is_bookmark`) and `on_ignore` has a `if meta.is_bookmark then return end` guard; ③ **bookmark pages can be favorited** — the long-press "收藏图片" (Favorite) routes through `addFavorite`'s `is_bookmark` branch: it renders a PNG into `bookmark_cache` and records a favorite with `is_bookmark=true`, visible both in the global Favorites gallery and the book's "收藏" (Favorites) tab; ④ the generated PNG is treated as **cache** — cleared by "清除本书图片缓存" (`_clearBookCache`, deletes by `<dochash>_` prefix and prunes this book's bookmark favorite records), plus a 64MB cross-book cold-file (mtime) eviction (`_pruneBookmarkDiskCache`). Rendering delegates to KOReader's built-in `ReaderThumbnail` subprocess (`getPageThumbnail`, signature verified against source) — no custom engine; a disk hit (`DataStorage:getDataDir()/artgallery/bookmark_cache/<dochash>_p{page}_w{w}_h{h}{_nm}.png`) loads instantly and skips the subprocess; otherwise a white placeholder is returned and, once the subprocess finishes, `ArtGalleryViewer:_onBookmarkThumbReady` rebuilds the gallery grid to swap in the real thumbnail (with a sync-path re-check to avoid a lingering placeholder). Supporting changes: ① `_collectBookmarkMetas` reads `self.ui.annotation`/`self.ui.bookmark` (skipping highlights via `a.drawer`), tagging metas with `is_bookmark=true`, `spine_index=page` as a reading-order approximation, sorted by page; ② `_ensureDerived` folds the bookmark render closures into `_render_of` so the Favorites derived list can show favorited bookmarks; ③ `_favoritesLists` scans `bookmark_metas`; ④ `_cycleGalleryFilter` order extended to all→favorites→bookmarks→ignored (only visible segments); ⑤ on viewer close, `onCloseWidget` calls `_freeBookmarkThumbs` to free cached copies and cancel in-flight render requests, preventing leaks; ⑥ About doc and settings item updated. Limitation: the Bookmarks tab is browse-only (bookmarks aren't in the single-image primary pool, so tapping a bookmark thumbnail is a no-op); favoriting is done via the long-press menu — this matches the requirements without coupling bookmarks into single-view progress/locate logic. Checks: syntax OK; `getBookmarkPageNumber`/`getPageThumbnail` signatures verified against KOReader source. Version stays **v1.0.19 (no bump)**; not pushed / not released (per the "release needs approval" rule); pending KPW3 on-device three-checks (Chinese source / switch-to-English all-English / framework strings normal) and real-device confirmation of bookmark render/favorite/cleanup.

- **自检补记（2026-08-16）**：进入验收技能「自检-修复循环」。静态深度精读全部书签相关接线（`_collectBookmarkMetas`/`_saveBookmarkThumbToDisk`/`_bmCachePath`/`addFavorite`/`_favoritesLists`/`_openImageActionMenu`/`on_ignore`/`onCloseWidget` 等），并对照 KOReader 源码核实数据模型（`readerbookmark.lua:397` 证书签落在 `self.ui.annotation.annotations`；`readerannotation.lua:16-96` 证纯页书签 `drawer=nil`、高亮才有 drawer；`readerthumbnail.lua:257/268/317` 证 `getPageThumbnail` 回调首个参数即带 `.bb` 的 tile，同步命中 line 268、异步 line 317）。捕获并修复 3 个真实缺陷：①【缓存文件名错位】`_saveBookmarkThumbToDisk` 原用 `bb:getWidth()/getHeight()` 命名，而 `_loadBookmarkThumbFromDisk` 用 `_bmThumbSize()` 去找 → 磁盘缓存永不命中；改为统一 `_bmThumbSize()` 并给 `_bmCachePath` 加 nil 兜底，使「存/读/收藏」三处路径一致。②【书签 path 键错乱】`_collectBookmarkMetas` 原用注解字段 `a.page` 拼 `path`，而 `im.page` 用 `getBookmarkPageNumber(a)`，二者可能不等 → 缓存/收藏键错位；改为统一用 `page`。③【meta 宽高失准】书签 meta 原用整屏尺寸作宽高，致瀑布流比例与占位图尺寸失准；改为 `_bmThumbSize()`。另核实：`on_ignore` 书签早返回双保险、`_openImageActionMenu` 对书签禁用「忽略」、`_favKeyFor`/`addFavorite`/`_favoritesLists` 三处收藏键一致（均 `doc.file .. "#" .. im.path`）、画廊点按书签经 `_primaryIndexForMeta` 返回 nil 安全无操作、`onCloseWidget` 内 `self` 捕获 ArtGallery 故 `_freeBookmarkThumbs` 正确释放。校验：LuaJIT 语法 `SYNTAX OK`。v1.0.19 维持未升版；未 push、未发版。

- **真机崩溃修复补记（2026-08-16）**：KPW3 实机点击「书签（1）」段致 KOReader 崩溃，`G:\koreader\crash.log` 定位 `main.lua:4426: attempt to call global '_mkdirs' (a nil value)`。**根因**：`_mkdirs` 是模块级 `local function`（定义于 4620 行），但其调用点 `_bmCacheDir`（4426）定义在它**之前**；Lua 词法作用域下，前面定义的方法看不到后面声明的 local，调用点解析到全局 `_mkdirs`（nil）而崩溃（`addFavorite` 的 4758 调用点在 4620 之后、本可正常解析，故该缺陷仅在「点书签段」这条 `_bmCacheDir→_bmCachePath→_mkdirs` 路径暴露）。**修复**：将 `_mkdirs` 改为 `ArtGallery:_mkdirs` 方法（经元表运行时查找，与定义顺序无关），并修正两处调用点为 `self:_mkdirs(dir)`（4426、4758）。**同类隐患排查**：Phase 38 区域其余私有 local（`_flatten_on_white` 4358 的调用 4401/4455 均在后；`_copy_file_bytes` 5007 仅被其后 `_migrateLegacyFavorites` 5040 调用）均无前向引用问题。校验：LuaJIT 语法 `SYNTAX_OK`；已 `cmp` 重部署 G 盘设备（`G_MAIN_IDENTICAL`），版本维持 **v1.0.19 未升版**、未 push、未发版。请重开本书图库→点「书签」段复验（预期：出现书签缩略图网格；异步渲染到位后自动替换占位）。

- **书签可跳转补记（2026-08-16）**：用户实机复验确认书签缩略图能渲染、不再崩溃，但指出「书签只有缩略图、点按无反应、无法全屏查看、不明白意义」——Phase 38 当初把书签写成「点按无操作」确属半成品。本次赋予书签明确交互意义：**书签段本质=可视化书签目录**，点缩略图即一键回到该书签页。**改动（main.lua）**：① 新增 `ArtGalleryViewer:_jumpToBookmark(meta)`——关闭画廊/查看器后把阅读位置跳到该书签页（跳前 `ui.link:addCurrentLocationToStack` 压栈，使阅读器「返回」能回到看图前位置）；按文档类型分流：滚动文档（EPUB）优先用 `bookmark_ref`（原始 xpointer）走 `ui.rolling:onGotoXPointer` 精确定位，分页文档（CBZ）走 `ui.paging:onGotoPage(meta.page)`，滚动回退走 `ui.rolling:onGotoPage`。跳转范式复用已验证的 `_showInBook`（`self:onClose()` 后跳转）。② `onTap` 画廊网格点击分支：书签 meta 改走 `_jumpToBookmark`（图片仍走原 `_exitGallery` 全屏查看），消除「点书签无操作」。③ 长按菜单 `_openImageActionMenu`：书签 meta 走独立菜单——新增「跳转到此页」置顶，保留「收藏图片/取消收藏」（满足要求③：书签页可被收藏，走 `addFavorite` 的 `is_bookmark` 分支、非损坏项），仅剔除「忽略图片」（满足要求②：书签永不进忽略池）。④ `_collectBookmarkMetas` 新增 `bookmark_ref = a.page`（原始引用，滚动文档为 xpointer 字符串、分页为页码数字），供精确跳页。校验：LuaJIT 语法 `SYNTAX_OK`；跳转逻辑对照插件既有分页/滚动跳页范式（`_syncReadingProgress` / `on_show_in_book`）核实一致；`ui.link`/`onClose` 均复用既有可用路径。版本维持 **v1.0.19 未升版**、未 push、未发版；待 KPW3 实机复验（点书签缩略图→应跳转并全屏呈现该书页；长按书签→弹「跳转到此页」）。

- **书签全屏看图+删除补记（2026-08-16）**：用户进一步要求书签「既能跳转、也能全屏看图」，并新增「长按删除书本书签」。依此前 AskUserQuestion 拍板（轻点=全屏看图/glimpse 风 + 高清重渲染；跳转放长按菜单）落地。**改动（main.lua）**：① `onTap` 画廊点按书签分支由「直接跳转」改为 `_viewBookmark(meta)`——进入单图全屏态把该书签当「伪图片」展示（复用既有缩放/全屏/⋮ 菜单基建，隔离在 `self._view_bookmark_meta` 标记内，真实图片路径不受影响）；初始以已渲染缩略图副本瞬时显示，再经新增 `ArtGallery:_renderBookmarkHiRes`（委托 `ui.thumbnail:getPageThumbnail` 按屏幕分辨率异步渲染）回调替换高清 BB（旧占位/副本显式释放，无重复 free——上游 `_clean_image_wg` 只释放 `_image_wg` 且 ImageWidget `image_disposable=false`）。② ⋮ 菜单新增书签态专属「跳转到此页」（不依赖 showinbook 快捷动作开关），并把通用「在书中定位」行在书签态隐藏以免重复；`_showInBook` 在单图态遇书签 meta 即改走 `_jumpToBookmark`（阅读器全屏呈现该书页）。③ 长按菜单新增「删除书签」——经 `ConfirmBox` 二次确认后调新增 `ArtGallery:_removeBookmark`（仿 glimpse v1.3.0 实证：遍历 `ui.annotation.annotations` 匹配 `meta.bookmark_ref` 精准定位单条纯页书签，调 `ui.bookmark:removeItem(a,i)` 双向删除书本身，并强制重绘 dogear 折角角使其即时消失），配套 `_removeBookmarkCacheFile` 清该条磁盘 PNG；删除后 `_refreshBookmarkMetas` 重建书签池并刷新网格，若正单图查看该书签则 `_exitBookmarkView` 回画廊（无书签回退「全部」）。④ 状态清零：`_enterGallery`/`_exitGallery` 开头清 `_view_bookmark_meta`，避免同轮打开中先看签再看真图导致的菜单残留误显。校验：LuaJIT 语法 `SYNTAX_OK`；删除 API 与 dogear 刷新对照 glimpse v1.3.0 源码（`_removeBookmark`/`_removeCurrentBookmark` 赖其 `removeItem` 语义）核实一致；高清渲染复用已验证的 `getPageThumbnail` 异步范式。版本维持 **v1.0.19 未升版**、未 push、未发版；已 `cmp` 重部署 G 盘（`G_MAIN_IDENTICAL`），待 KPW3 实机复验（轻点书签→全屏看图、点 ⋯/长按→跳转到此页/删除书签）。

- **长按书签菜单崩溃修复补记（2026-08-16）**：KPW3 实机**长按书签缩略图**致 KOReader 崩溃，`G:\koreader\crash.log` 定位 `main.lua:2619: attempt to index global 'menu' (a nil value)`（发生于 `anchor` 闭包被 `movablecontainer:ensureAnchor` 调用时）。**根因**：书签分支的长按菜单写成单行 `local menu = ArtGalleryPopupMenu:new{ ... anchor = function() ... menu.movable ... end }`；Lua 词法作用域规定局部变量只在其**声明语句之后**才进入作用域，单行声明里初始化表达式 `<expr>` 中的 `menu` 仍解析为**全局**（正在声明的局部尚未激活），于是 `anchor` 闭包捕获的是全局 `menu`（nil），菜单 paint 时对 nil 索引而崩。**对比**：非书签分支（2693-2694）用的是 `local menu`（先独立声明）+ `menu = ArtGalleryPopupMenu:new{...}`（再赋值）两步分离，初始化表达式在声明激活之后，故 anchor 能正确捕获局部 `menu`——这正是它不崩的原因。**修复**：把书签分支的 `local menu` 提前到 `items` **之前**声明（使 `items` 内「删除书签」回调的 `UIManager:close(menu)` 与 `anchor` 闭包都绑定同一局部 `menu`），并改构造为 `menu = ArtGalleryPopupMenu:new{...}`（去掉声明）。**校验**：LuaJIT 语法 `SYNTAX OK`；已 `cmp` 重部署 G 盘（`G_MAIN_IDENTICAL`），版本维持 **v1.0.19 未升版**、未 push、未发版。请重开本书图库→「书签」段→**长按**书签复验（预期弹菜单：跳转到此页/收藏·取消收藏/删除书签，不再崩溃）；此坑已记入项目记忆（Lua 局部作用域前向引用/单行声明陷阱）。

- **书签删除落盘 + 书签池⭐角标修复补记（2026-08-16）**：用户实机复验回报两个问题。①「长按删除书签无效、书签依然存在」——根因为**删除未落盘**：`bm:removeItem` 仅改内存中的 `ui.annotation.annotations`，磁盘持久化原本依赖关书时的 `ReaderAnnotation:onSaveSettings`；若删除后未正常关书/强退/重开本书，书签即从本书 `.sdr` 恢复。修复：`ArtGallery:_removeBookmark` 在 `removeItem` 之后立即 `pcall(self.ui.annotation:onSaveSettings())` 强制落盘（`_invalidateGalleryCaches` 本就会清 `_gallery_layouts`/`_thumb_bbs`，故网格刷新本就正常，问题仅在持久化）。②「被收藏的书签在『书签（N）』池无 ⭐」——根因为 `_buildGallery` 的 `show_corner` 仅对 `f=="all"` 开放（`main.lua:2967`），书签池（`f=="bookmarks"`）被排除，故既无 ⭐ 也无 `_cornerLuma` 反白。修复：`show_corner` 改为 `f=="all" or f=="bookmarks"`，书签池的收藏项即显示右下角 ⭐ 且沿用角点偏暗自动反白（与「全部」视图一致）；收藏判定复用 `isFavoriteByKey(_favKeyFor(meta))`，书签收藏键 `doc.file#artgallery-bm:<page>` 与 `addFavorite` 写入键同源，匹配无误。校验：LuaJIT 语法 `SYNTAX OK`；已 `cmp` 重部署 G 盘（`G_MAIN_IDENTICAL`），版本维持 **v1.0.19 未升版**、未 push、未发版。待 KPW3 复验：长按书签→删除→确认网格该缩略图消失且**重开本书后不再恢复**；收藏某书签→「书签（N）」池该缩略图右下角出现 ⭐（暗角自动反白）。

- **书签删除落盘真正修复 + ⭐角标精准定位补记（2026-08-16 深夜）**：用户实机复验回报上一轮修复「删除仍无效、⭐仍超出缩略图」——说明上一轮根因方向对、但落点错，已回源码复核纠正。① **删除仍无效的真因**：`ReaderAnnotation:onSaveSettings()`（readerannotation.lua:249）内部仅 `self.ui.doc_settings:saveSetting("annotations", self.annotations)`，**自身不 flush**——`saveSetting` 只改内存中的 settings 表，必须显式 `doc_settings:flush()` 才写回磁盘 `.sdr`；上一轮调 `onSaveSettings` 后数据从未落盘，重开本书即恢复。另 `bm:removeItem` 此前未 pcall 保护，抽屉态 reader `footer` 等状态异常时可能抛错中断删除。修复（`ArtGallery:_removeBookmark`）：a. `pcall(bm:removeItem(a,i))` 包裹内存删除；b. 删除后**二次校验**内存确已无匹配项（removeItem 内部 footer 抛错但 `table.remove` 已生效的情形也能识别），未删掉则直接返回 false；c. 显式 `ann:updatePageNumbers()` + `self.ui.doc_settings:saveSetting("annotations", ann.annotations)` + `self.ui.doc_settings:flush()` 强制即时落盘（不再依赖关书）；d. dogear 折角重绘仍 pcall 保护。② **⭐超出缩略图真因**：角标原锚定在 **cell 角**（`c.x + c.w - off`），但缩略图经 `_thumb` fit 进 `(c.w-2·inset)×(c.h-2·inset)` 框后实际宽 `bw < c.w`（书签固定比例图在较宽列里必然窄），图片右下角真实落在 `c.x + (c.w+bw)/2`，比 cell 右角内缩 `(c.w-bw)/2`——列宽 > 图宽时该内缩显著，角标便悬空在图片之外。修复：`CenterContainer` 把 bb 居中在 cell 内，故图片左上角精确为 `c.x+(c.w-bw)/2, c.y+(c.h-bh)/2`（FrameContainer 单边边距在居中计算中被数学抵消，定位精确），角标锚定到该真实图片右下角内缩 `Screen:scaleBySize(4)`，所有视图（全部/书签）一律精准贴图、不再悬空；原 `off` 局部变量已弃用删除。校验：LuaJIT 语法 `SYNTAX_OK`；已 `cmp` 重部署 G 盘（`G_MAIN_IDENTICAL`），版本维持 **v1.0.19 未升版**、未 push、未发版。请 KPW3 实测：① 长按书签→删除→网格该缩略图立即消失，且**退出本书再重开，该书签确实不再出现**（验证已落盘）；② 收藏某书签→「书签（N）」池该缩略图右下角 ⭐ 精准贴于图片右下角（不再悬空/超出）。

- **书签删除彻底兜底修复补记（2026-08-16 深夜二轮）**：用户实机复验回报上一轮「删除仍无效」——本轮先读取真机取证：`G:\Books\神秘复苏.sdr\metadata.epub.lua` 时间戳仍为 `10:34`、早于本轮 20:21 启动，且 annotations 仅 1 条、其 `page` 字段是 **xpointer 字符串**（如 `/body/DocFragment[490]/...`）；而画廊重建链路（`_refreshBookmarkMetas`→`update`→`_buildGallery`→`_tabList` 取 `bookmark_metas`）本身通畅。由此锁定根因：**删除未落盘，且非"匹配失败"（xpointer 同源必匹配），而是运行时 `ui.bookmark:removeItem(a,i)` 未能真正把该项从 `ui.annotation.annotations` 移除**，导致二次校验 `still` 为 true → 提前 return → 跳过 `flush` → 磁盘未更新。**修复（`ArtGallery:_removeBookmark`）**：a. 先遍历定位匹配项索引 `idx`（`not a.drawer and a.page == match`）；b. 记录 `before = #ann.annotations`，`pcall(bm:removeItem(ann.annotations[idx], idx))` 优先走 KOReader 原生删除（维护 dogear/footer/事件）；c. **兜底**：若 `#ann.annotations == before`（原生删除未能减少数量，任意原因中断），直接 `table.remove(ann.annotations, idx)` 保证内存移除，杜绝"删除无效、落盘后书签复活"；d. **无论如何强制落盘**（不再因 `still` 提前 return）：`ann:updatePageNumbers()` + `saveSetting("annotations", ann.annotations)` + `flush()`；e. 加 `logger.info("[ArtGallery] _removeBookmark: ...")` 关键路径诊断（matched index / flushed / fallback），真机复验若仍异常可从 `G:\koreader\crash.log` 取证。**校验**：LuaJIT 语法 `SYNTAX_OK`；已同步工作副本≡干净副本（cmp IDENTICAL）。版本维持 **v1.0.19 未升版**、未 push、未发版。**待办**：经用户授权后 `cmp` 重部署 G 盘，请用户在真机删除任一书签→**立即退出 KOReader**→我读取该书 `.sdr/metadata.*.lua` 时间戳确认已从 10:34 更新、且 annotations 已不含该书签，验证落盘闭环。

- **书签删除输入穿透修复 + nextTick 补记（2026-08-16）**：用户实机复验回报轮6（兜底 table.remove + 强制 flush）「删除仍无效」，且零 `[ArtGallery]` 日志。本轮读取真机 `G:\koreader\crash.log` 取证：仅 1 条 `[ArtGallery] DEL_MENU_ITEM tapped`（用户点了「删除书签」），其后到重启无任何 `DEL_CONFIRM` / 报错，`metadata.epub.lua` 时间戳仍停在 10:34 → 断点卡在「菜单项已点、确认框 OK 回调从未触发」。**根因**：KOReader 菜单项 `callback` 在菜单自身输入流程中**同步执行**，此时同步 `UIManager:show(ConfirmBox)` 会被本次点按事件「穿透」（误触取消/背景，或菜单关闭把确认框一起弹掉）→ 确认回调永远走不到（前 6 轮「删除无效」且零 `DEL_CONFIRM` 即此因）。**修复**：把 `UIManager:show(ConfirmBox)` 整段包进 `UIManager:nextTick`，推迟到本轮输入事件结束、下一 UI 帧再弹，消除穿透/竞态（`nextTick` 已在 koreader 源码 `frontend/ui/uimanager.lua:354` 核验存在）；并加 `DEL_CB_SHOWN`（框已弹）/ `DEL_CANCEL`（点取消）入口日志便于定位。校验：LuaJIT 语法 `SYNTAX_OK`。版本维持 **v1.0.19 未升版**、未 push、未发版。

- **书签删除确认框字段名修复补记（2026-08-16）**：轮7 nextTick 让确认框能弹（真机日志出现 `DEL_CB_SHOWN`），但点「删除」仍无反应——`DEL_CONFIRM` 始终不触发。查 koreader 源码 `frontend/ui/widget/confirmbox.lua:115-122`：**ConfirmBox「确定」按钮调用的是 `self.ok_callback()`，构造参数名是 `ok_callback`**；而删书签代码误写成 `callback = function()...`，ConfirmBox 不认该字段，故「删除」走默认空 `ok_callback`（啥也不做）。全仓核查其余 5 处 ConfirmBox（1236/5433/5661/5676/6855）均正确用 `ok_callback`/`cancel_callback`，仅此一处误用。**修复**：OK 动作字段 `callback` → `ok_callback`。这棵「字段名错」此前架空了轮5/6/7 的全部落盘与穿透修复，修正后删除方真正生效。校验：LuaJIT 语法 `SYNTAX_OK`。版本维持 **v1.0.19 未升版**、未 push、未发版。

- **书签删除修复确认生效 + 诊断代码清理补记（2026-08-16）**：用户实机确认删除生效。随后清理前期 5~9 轮「误诊」期间为定位根因临时埋的 8 条诊断日志（`DEL_MENU_ITEM`/`DEL_CB_SHOWN`/`DEL_CONFIRM`/`DEL_CANCEL`/`CALLED`/`matched index`/`fallback table.remove`/`flush ok=`），仅保留 3 条真实错误告警（`bail, refs missing`/`no annotation matches`/`flush` 仅失败时告警）。全部正确业务逻辑（兜底删除 + 强制落盘 + `updatePageNumbers` 与落盘解耦 + `nextTick` + `ok_callback`）原样保留。校验：LuaJIT 语法 `SYNTAX_OK`；三副本一致；经 `koplugin-selfcheck` 技能自检 19/19 全绿；`koplugin-merge-acceptance` 验收五项达标（含 `G:/koreader/crash.log` 取证 `flush ok=true left=0` 落盘闭环铁证）。版本维持 **v1.0.19 未升版**、未 push、未发版。

- **版本升至 v1.0.20 并发版（2026-08-16）**：阶段三十八「书签入画廊」自初版（轻点全屏看图 + 长按跳转/删除）经多轮 KPW3 真机复验与根因定位——崩溃修复 → 落盘兜底 → `nextTick` 消除输入穿透 → `ok_callback` 字段名修正 → 诊断清理——书签删除 / 收藏 / 跳转 / 缓存清理全链路在 KPW3 实机确认稳定。据此将插件版本由 **v1.0.19 升至 v1.0.20** 并建立 GitHub Release（latest），发行包 `artgallery.koplugin-v1.0.20.zip` 镜像既有结构（根 `artgallery.koplugin/`，含 `main.lua`/`_meta.lua`/`artgallery_scanner.lua`/`assets`/`README.md`/`CHANGELOG.md`/`audit`，不含 `.git`/`Backup`）。仓库代码、`audit/CHANGELOG.html`、本 `.md` 三者同步至 v1.0.20；发版经用户明确授权（依「发版须经同意」纪律）。

## 阶段三十七 / Phase 37 — 文档修正：「相比上游的改进」改为分别对比两上游最新版（2026-08-15）
- **中文**：用户指出 README「相比上游的改进」板块应**分别**对比 Glimpse 与 Illustrations 两个插件的**最新版**，而非把二者混为一列、并据此错误地把上游特性写成弱点。已上 GitHub 核实两上游真实版本与功能：Glimpse 最新 `main` **v1.2.5**（最新 Release v1.2.0，2026-07-29）——其 README 证实它**本就**具备可配置最大放大倍数（150%–400%、默认 200%）、逐手势开关（双击/滑动/捏合各自可关）、以及同款「更聪明过滤」引擎（参考图名识别 + 图文书自动放宽）；Illustrations 最新 **v0.5.2**（2026-04-25）——较精简，仅「最小图片尺寸」阈值过滤、全局收藏、防剧透两档、手势分配、无忽略池。据此重写该板块为**两个独立子对比**（对比 Glimpse v1.2.5 / 对比 Illustrations v0.5.2），并诚实标注每项是「同源/均具备」还是「美术馆新增/增强」：① 纠正旧表把"上游固定缩放上限/手势固定不可关/基础过滤"当成 Glimpse 弱点的失实（实为 Glimpse 既有能力，美术馆继承）；② 纠正"收藏收为书内"的失实——经核查 `ArtGallery:_favSettings()` 收藏存于 `artgallery/favorites.lua`，是**全局收藏**，与 Illustrations 同源；真正差异是 Illustrations **无忽略池**且收藏为独立画廊，美术馆新增**忽略池**并把三池统一进分段切换器；③ 保留真实差异项（全中文界面、三态填充、CBZ 入口、统一分段切换器、可选书籍进度主动同步、Glimpse 不推进进度而美术馆可选推进等）。仅文档改动，无运行行为变化；未重新发版，仓库 README 已更新（发行包 zip 内 README 仍为旧表述，如需可重新发版）。
- **English**: User noted the "Improvements over the upstreams" section should compare **Glimpse** and **Illustrations separately** against their **latest versions**, not merge them into one column and (thereby) mislabel upstream strengths as weaknesses. Verified both upstreams on GitHub: Glimpse latest `main` **v1.2.5** (latest Release v1.2.0, 2026-07-29) — its README confirms it **already** has a configurable max zoom (150%–400%, default 200%), per-gesture toggles (double-tap/swipe/pinch each off), and the same "smarter filter" engine (reference-name recognition + illustrated-book relax); Illustrations latest **v0.5.2** (2026-04-25) — leaner, only a min-image-size threshold filter, global favorites, two spoiler scopes, gesture assignment, no ignore pool. Rewrote the section into **two separate sub-comparisons** (vs Glimpse v1.2.5 / vs Illustrations v0.5.2) and honestly tagged each row as "same / both have" or "ArtGallery added / enhanced": ① corrected the old table's false claim that "fixed zoom ceiling / fixed gestures / basic filter" were Glimpse weaknesses (they are Glimpse's existing capabilities, inherited); ② corrected the false "favorites scoped in-book" — `ArtGallery:_favSettings()` stores favorites in `artgallery/favorites.lua`, i.e. **global favorites**, same as Illustrations; the real difference is Illustrations has **no ignore pool** and keeps favorites as a separate gallery, whereas ArtGallery adds the **Ignore pool** and unifies the three pools into the segmented switcher; ③ kept the genuine differentiators (fully Chinese UI, three fit modes, CBZ entry, unified segmented switcher, optional active book-progress sync, Glimpse never advancing progress vs ArtGallery optionally doing so). Docs only, no runtime change; no new release, repo README updated (the release-zip asset still carries the old wording, re-cuttable on request).

## 阶段三十六 / Phase 36 — 文档修正：更正「支持格式」声明（2026-08-15）
- **中文**：用户严格核对 README 声称的支持格式时发现，README 多处声明「**CBZ / DjVu / PDF** 分页文档」与「**EPUB + CBZ / DjVu / PDF**」受支持，与代码实际行为不符。经核查代码：① `_supportedReason()`（main.lua:4025-4039）对没有 `getDocumentFileContent` 的文档（即 PDF / DjVu 这类分页格式）一律返回「美术馆仅支持 EPUB 格式」，in-book 画廊（`showViewer`）只经 `scanner.scan` 解析 EPUB 的 container/opf，PDF / DjVu **不被扫描**；② **CBZ** 仅经文件管理器「浏览 CBZ 漫画」独立入口（`showCbz` → `scanner.scan_cbz`）全屏翻阅，并非 in-book 画廊，且不做相关性过滤；③ 全代码无 `showPdf` / `showDjvu` 入口。故原 README 属事实性错误，已更正：**「支持格式」章节改为按场景说明——EPUB（内置画廊原生强项）/ CBZ（漫画独立入口）/ PDF·DjVu（暂不支持，明确提示）；** 进度同步小节与「相比上游」对比表中「EPUB + CBZ / DjVu / PDF」一并更正为「EPUB（内置画廊）+ CBZ（漫画独立入口）；PDF / DjVu 暂不支持」。另同步修正 main.lua 两处会误导人的注释（:4108 / :4160 的「CBZ/DjVu/PDF」并列，标注 PDF / DjVu 已被 `_supportedReason` 拦截、不会走到进度同步逻辑）。仅文档与注释改动，**无运行行为变化**；main.lua 语法 `SYNTAX_OK`。不重新发版（文档修正），仓库 README 已更新；v1.0.19 发行包 zip 资产内 README 仍为旧表述，如需可重新发版。
- **English**: User strictly audited the formats README claimed and found it stated "**CBZ / DjVu / PDF** paged documents" and "**EPUB + CBZ / DjVu / PDF**" as supported — which does not match the code. Verification: ① `_supportedReason()` (main.lua:4025-4039) rejects any document lacking `getDocumentFileContent` (i.e. PDF / DjVu paged formats) with "ArtGallery only supports EPUB"; the in-book gallery (`showViewer`) only calls `scanner.scan` (parses EPUB container/opf), so PDF / DjVu are **never scanned**; ② **CBZ** is reachable only via the separate file-manager "Browse CBZ comics" entry (`showCbz` → `scanner.scan_cbz`) full-screen browser, not the in-book gallery, and skips relevance filtering; ③ there is no `showPdf` / `showDjvu` entry at all. The old README was factually wrong and is now corrected: the **"Supported formats" section is rewritten by use-case — EPUB (in-book gallery, native strength) / CBZ (comic entry) / PDF·DjVu (not supported yet, clearly flagged);** the progress-sync paragraph and the "vs upstream" comparison table's "EPUB + CBZ / DjVu / PDF" are also corrected to "EPUB (in-book gallery) + CBZ (comic entry); PDF / DjVu not yet supported". Also fixed two misleading comments in main.lua (:4108 / :4160 listed "CBZ/DjVu/PDF", now noting PDF / DjVu are blocked by `_supportedReason` and never reach the progress-sync logic). Docs and comments only — **no runtime behavior change**; main.lua syntax `SYNTAX_OK`. No new release (doc fix); the repo README is updated, but the v1.0.19 release-zip asset still carries the old wording and can be re-cut on request.

## 阶段三十五 / Phase 35 — 双池带实时计数的分段切换器（吸收母插件 glimpse v1.2.5 思路）（2026-08-15）
- **中文**：v1.0.19。用户按「③→①→②」顺序吸收母插件 glimpse v1.2.5 可借鉴项，③（手势开关）、①（更聪明过滤）已先后完成并彻底核验，本阶段实施 ②（双池带实时计数的分段切换器）。改动（main.lua）：① 新增 `ArtGallerySegmented` widget（紧接 ArtGalleryTextButton 之后），三段式直达选择器——每段显示实时计数 `全部[N]/收藏[F]/忽略[M]`，当前段反相（黑底白字）以表选中，整体拉伸填满底栏为等高单元；② `_buildPill` 画廊分支由旧的「三态循环按钮」改为构建该分段控件——计数来源 `全部 = #shown + #ignored`、`收藏 = #_favoritesLists`（走 `_fav_cache` 无性能回归）、`忽略 = _ignoredCount()`；且<b>仅当确有忽略项（`_hasIgnoredTab()`）才显示「忽略」段</b>，无忽略时只有 全部/收藏 两段，与旧版外观/行为一致；③ 新增 `_selectGalleryFilter(key)` 方法，点按分段直接切到对应池（替代 onTap 画廊 pill 命中处的 `_cycleGalleryFilter` 循环），含两道 guard——点当前段无操作、无忽略项时不可选忽略；④ onTap 画廊 pill 命中改用 `segmentKeyAt(ges.pos.x)` 判定命中段；⑤ `_buildPill` 额外兜底：当处于「忽略」视图但已无忽略项时自动回退「全部」，避免空画廊。`_cycleGalleryFilter` 仍保留供 ⋯ 菜单入口（行为不变）。约 110 行（含 widget），零新增性能压力（收藏计数走缓存）。<b>校验捕获并修复一处真 bug</b>：初版 `_buildPill` 用 `local _, fav_metas = ...` 承接首返回值，会遮蔽 gettext 的 `_()`，而该作用域内紧随其后的 `_("全部")` 调用将在真机崩（attempt to call a table value）；依本插件既有约定（见 `_tabList` 处注释）改名 `_fav_list` 修复。完整核验：LuaJIT 语法 `SYNTAX OK`、类顺序 OK、代理加载 `LOAD_OK`、local _ 遮蔽检查（修复后无）、新增 headless 单元测试 `D:/TeleAgent/test_segmented.lua` 抽取真实 widget 代码（轻量 KOReader 桩）跑 24 项断言全过（几何/拉伸均分/命中边界含偏移/paintTo 不报错/重复 setWidth 安全）。待 KPW3 实机复测；未 push、未发版（依「发版须经同意」纪律）。
- **English**: v1.0.19. Per the ③→①→② absorption order from upstream glimpse v1.2.5, Phases ③ (gesture toggles) and ① (smarter filter) are done and verified; this phase implements ② (dual-pool segmented switcher with live counts). Changes (main.lua): ① new `ArtGallerySegmented` widget (right after `ArtGalleryTextButton`) — a three-segment direct selector, each segment shows a live count `全部[N]/收藏[F]/忽略[M]`, the active segment is inverted (black fill, white text) to show selection, and it stretches to fill the bottom bar as equal-height cells; ② `_buildPill`'s gallery branch now builds this control instead of the old cycling button — counts come from `all = #shown + #ignored`, `favorites = #_favoritesLists` (via `_fav_cache`, no perf regression), `ignored = _ignoredCount()`; the `忽略` (Ignored) segment appears only when there actually are ignored items (`_hasIgnoredTab()`), so with none ignored only 全部/收藏 show, matching old behavior; ③ new `_selectGalleryFilter(key)` switches straight to the tapped pool (replacing the onTap `_cycleGalleryFilter` cycle), with two guards — tapping the current segment is a no-op, and Ignored is unselectable when empty; ④ onTap now uses `segmentKeyAt(ges.pos.x)` to find the hit segment; ⑤ `_buildPill` also falls back to "all" if the current filter is "ignored" but nothing is ignored, avoiding an empty gallery. `_cycleGalleryFilter` is kept for the ⋯-menu entry (unchanged). ~110 lines (incl. widget), zero added runtime overhead (favorite count uses cache). <b>Verification caught and fixed a real bug</b>: the first version wrote `local _, fav_metas = ...` which shadowed gettext's `_()`; the `_("全部")` call right after it in the same scope would have crashed on-device (attempt to call a table value). Renamed to `_fav_list` per the plugin's existing convention. Full checks: syntax OK, class-order OK, load LOAD_OK, no local _ shadowing, and a new headless unit test `D:/TeleAgent/test_segmented.lua` extracting the real widget (light KOReader stubs) passing 24 assertions (geometry / equal-cell stretch / hit boundaries incl. offset / paintTo non-error / repeated setWidth safety). Pending on-device (KPW3) retest; not pushed / not released.

## 阶段三十四 / Phase 34 — 更聪明的相关性过滤（吸收母插件 glimpse v1.2.3）（2026-08-15）
- **中文**：v1.0.18。用户对比母插件 glimpse v1.2.3 后，按「③→①→②」顺序在 ③ 之后实施 ①（更聪明过滤）。纯扫描逻辑增强，不依赖页面渲染，运行期零压力。改动（artgallery_scanner.lua）：① 新增 `M.reference_name(path)`（:494）按文件名识别参考图（map/family-tree/pedigree/diagram/chart/graph/timeline/schematic/infographic，词边界匹配避免 remap/photo/logo 误命中），在 `M.filter` 的 `captioned` 判定（:937-939）中赋予与 figcaption 同等 relief（半尺寸门槛 + 50% 长宽比宽容），使小尺寸/异形地图、族谱、图表不再被 small/aspect 误杀；② 图文非虚构自动放宽（:850 常量 + :908-930 预扫描 + :997 size_relief）——统计全书「强参考信号」密度（真实说明/出版 figure 名/参考图名/自身已够大），当 `strong ≥ 4` 且占全部图片 `≥ 40%` 时判定为图文书，对无说明小图放宽尺寸下限至 `INFORMATIVE_RELIEF=0.6`，但<b>长宽比测试保持严格</b>（高瘦装饰图仍丢弃）；③ `M.VERSION` 5→6（:29）使旧缓存失效、升级后首次打开自动重扫。校验：LuaJIT 语法 `SYNTAX OK`、类顺序 OK、代理加载 `LOAD_OK`、headless 单元测试 `M.filter` 16 项断言全过（参考图识别/无回归/图文非虚构放宽/strict·relaxed 行为）。待 KPW3 实机复测。
- **English**: v1.0.18. Per the ③→①→② order, Phase ① (smarter relevance filter, from glimpse v1.2.3) follows ③. Pure scan-logic enhancement, no page rendering, zero runtime overhead. Changes (artgallery_scanner.lua): ① new `M.reference_name(path)` (:494) recognizes reference images by filename (map/family-tree/pedigree/diagram/chart/graph/timeline/schematic/infographic, word-boundary matched so remap/photo/logo don't false-positive), granting them the same caption relief in `M.filter`'s `captioned` test (:937-939) so small/odd-shaped maps, family trees and charts survive the size/aspect floor; ② illustrated-book auto-relax (:850 consts + :908-930 pre-pass + :997 size_relief) — counts "strong reference signals" density (real caption / publisher figure name / reference name / already large); when `strong ≥ 4` and `≥ 40%` of all images, the book is deemed illustrated and uncaptioned small images keep at `INFORMATIVE_RELIEF=0.6`, but the aspect test stays strict (tall thin chrome still dropped); ③ `M.VERSION` 5→6 (:29) invalidates old caches so books auto re-scan on first open after upgrade. Checks: syntax OK, class-order OK, load LOAD_OK, headless unit test `M.filter` 16 assertions all pass. Pending on-device (KPW3) retest.

## 阶段三十三 / Phase 33 — 手势独立开关子菜单（吸收母插件 glimpse v1.2.5 思路）（2026-08-15）
- **中文**：v1.0.17。用户对比母插件 glimpse 更新到 v1.2.5 后，要求按「③→①→②」顺序吸收可借鉴项；③（手势独立开关）评估为低难度（纯 wiring、无新 widget / 无 layout 改动），先行实施，且每阶段做完彻底核验后再推进。改动：① 新增三个手势设置键 `artgallery_gesture_double_tap`（双击放大）/ `artgallery_gesture_swipe_nav`（滑动翻页）/ `artgallery_gesture_pinch_zoom`（捏合缩放），默认均开启（main.lua:80 附近）；② 插件菜单新增「手势」子菜单（:5693-5731），三项 checkbox 实时反映开关态、即时落盘；③ 三处手势入口加 guard——`onArtGalleryDoubleTap`（:3053）判关则不缩放，`onZoomIn`/`onZoomOut`（:3088/:3104）判关则不进入缩放（含拉伸态填满进入缩放路径），`onSwipe`（:3355）判关后画廊/适配态不再翻页但保留「适配态吞掉滑动、防上游误关查看器」的安全保护。纯配置+wiring，约 60 行，零新增渲染路径、运行期零额外压力，不引入 e-ink 残影源。校验：LuaJIT 语法 `SYNTAX OK`、类顺序检查 OK、代理加载 `LOAD_OK`、local _ 遮蔽检查无。待 KPW3 实机复测。
- **English**: v1.0.17. Per the user's request to absorb upstream glimpse v1.2.5 improvements in the order ③→①→②, Phase ③ (per-gesture toggles) was implemented first (low difficulty, pure wiring, no new widget / no layout change) and fully verified before moving on. New setting keys `artgallery_gesture_double_tap` / `artgallery_gesture_swipe_nav` / `artgallery_gesture_pinch_zoom` (default on, main.lua:~80); a new "手势" plugin-menu submenu (:5693-5731) with three live checkboxes; and guards at three gesture entry points — `onArtGalleryDoubleTap` (:3053, no zoom when off), `onZoomIn`/`onZoomOut` (:3088/:3104, no zoom when off, including the stretch-fill zoom-in path), and `onSwipe` (:3355, no page-swipe when off but keeping the fit-state "swallow swipe to avoid upstream closing the viewer" safety). Pure config+wiring (~60 lines), no new render path, zero runtime overhead, no e-ink ghost source. Checks: syntax OK, class-order OK, load LOAD_OK, no local _ shadowing. Pending on-device (KPW3) retest.

## 阶段三十二 / Phase 32 — 最大放大倍数可配置（吸收母插件 glimpse v1.2.2）（2026-08-13）
- **中文**：v1.0.16。吸收母插件 glimpse v1.2.2 的「可配置最大缩放」能力（对比报告 `D:\Download\artgallery_vs_glimpse_compare.html`）。改动：① 新增设置键 `artgallery_max_zoom`，`ArtGalleryViewer:init()`（:1000）读取（默认 1.5 保持旧行为）；② 插件菜单新增「最大放大倍数」子菜单（radio：1.5×/2.0×/2.5×/3.0×/4.0×，对齐母插件 150%–400%，:5661-5667），`text_func` 实时显示当前值；③ `_maxScale()`（:3572）抽屉态上限取该值，全屏态 `×2` 逻辑不变。纯配置改动，零新增渲染路径、零回归风险，改动约 20 行。校验：LuaJIT 语法 `SYNTAX OK`。实机（KPW3）测试通过（2026-08-13），已授权发版。
- **English**: v1.0.16. Absorbed the configurable max-zoom from upstream glimpse v1.2.2 (compare report `D:\Download\artgallery_vs_glimpse_compare.html`). New setting `artgallery_max_zoom` read in `init()` (:1000, default 1.5 preserves old behavior); new plugin-menu submenu "最大放大倍数" (radio 1.5×–4.0×, :5661-5667) with a live `text_func`; `_maxScale()` (:3572) uses it as the drawer cap while fullscreen keeps ×2. Config-only change (~20 lines), no new render path, no regression risk. Syntax OK. Released on 2026-08-13 after passing on-device (KPW3) testing.

## 阶段三十一 / Phase 31 — 验收整改：补 logger.dbg 关键路径日志 + 主动 collectgarbage（2026-08-11）
- **中文**：v1.0.15。依「合并插件验收标准」静态体检后的待整改项（二.4 / 五.1）执行 hardening。① 关键路径日志：在 `init`（:3711）、网络更新检查开始/结束（_runUpdateCheck :5356/:5361）、配置持久化（`_safeFlush` :3973、`_saveFavRecords` :4146）补 `logger.dbg` 埋点，便于真机按 `[ArtGallery]` 过滤取证（debug 级别，常态静默）。② 主动内存释放：在全屏 viewer 拆除（onCloseWidget :5086 置 nil 后）与一次完整扫描结束（_getScan :4040 后）两处安全点显式 `pcall(collectgarbage, "collect")`，及时回收全屏位图（≈0.8MB/张）与扫描临时表，缓解 KPW3 低内存压力。③ 源码复核纠偏：技能 pending 项"2.2 进度保存防抖"经核查不成立——`_syncReadingProgress`（:3905）本身不写 settings，仅经核心推进阅读位置；唯一落盘是 `on_image_shown` 的 `doc_settings:saveSetting("artgallery_last")`(:4912)，仅画廊内翻新图触发一次，底层持久化由 KOReader 核心节流，故由硬 ⚠ 降级为可选优化，不在本轮改动。三项校验（语法/类顺序/加载）全过。
- **English**: v1.0.15. Remediation of acceptance pending items (2.4 / 5.1). ① Key-path logging: added `logger.dbg` at init (:3711), network update-check start/end (_runUpdateCheck :5356/:5361), and persistence chokepoints (`_safeFlush` :3973, `_saveFavRecords` :4146) for on-device forensics via `[ArtGallery]` filter (debug level, silent normally). ② Active GC: explicit `pcall(collectgarbage,"collect")` at two safe points — fullscreen viewer teardown (onCloseWidget :5086 after nil-ing) and after a full scan (_getScan :4040) — to promptly reclaim fullscreen bitmaps (~0.8MB each) and scan temporaries on low-memory KPW3. ③ Source review corrected the pending "2.2 progress-save debounce": `_syncReadingProgress` (:3905) doesn't write settings itself; the only write is `doc_settings:saveSetting("artgallery_last")` on image-shown (:4912), fired once per gallery navigation, with core-throttled persistence — downgraded from hard ⚠ to optional, not changed this round. All three checks (syntax / class-order / load) pass.

## 阶段三十 / Phase 30 — 关于文档菜单项配图标（轻量 Unicode）+ 眼睛 emoji 占位（2026-08-11）
- **中文**：v1.0.14。用户实机确认 v1.0.13 关于弹窗已稳定弹出；要求「关于」文档各带图标菜单项配真实图标（凡有图标菜单必配）。核验：插件菜单图标实为 `assets/*.svg`，而 TextViewer 用 `x_smallinfofont`=NotoSans-Regular 渲染，无法内联 SVG / 不渲染彩色 emoji（KOReader 缺 emoji 字体易变方框）→ 采用轻量 Unicode 近似（⤢▦◎↻↺➤↩☑⭐）。眼睛行加 👁 图标 + 保留文字「划掉眼睛」，依赖用户自行安装 emoji 字体渲染。校验 SYNTAX OK。用户授权"不升版本号、改完直接更新发行版" → 已发 GitHub Release v1.0.14（含 BLC 修复 + 图标文档）。
- **English**: v1.0.14. Confirmed v1.0.13 About popup is stable. Added lightweight Unicode glyphs (⤢▦◎↻↺➤↩☑⭐) approximating each menu icon in the About doc (TextViewer uses NotoSans-Regular, can't inline SVG/emoji). Eye row gets 👁 + "划掉眼睛" text (needs user-installed emoji font). User authorized direct release update without bumping version → GitHub Release v1.0.14 published.

## 阶段二十九 / Phase 29 — 根治「关于」弹窗：锁定真凶 BookLoadCover Plus（2026-08-11）
- **中文**：v1.0.13。KPW3 真机带调用栈日志彻底锁定「关于」弹窗白闪/无弹窗的真凶——同机安装的 BookLoadCover Plus 补丁（`patches/2-bookloadcover-plus.lua`）。其 `patchUIManagerShow`（:1203-1219）在 `shouldSuppressClosingNotice()` 为真时，对文本同时含「关闭」+「书」的 widget 命中 `textLooksLikeClosingBookNotice`（中文判定 `{ "关闭", "书" }`，:486），将 widget 的 `paintTo` 清空为 `function() end`、`visible=false` 静默吞掉（onShow 仍触发、paintTo 永不调用 → 看不见弹窗）。本「关于」长文本恰含「关闭全部」「在书中定位」命中误判。→ 推翻阶段二十八「残留 tap 误关」结论（v1.0.12 假设被此真因掩盖）。修复：移除 v1.0.12 的 scheduleIn(0.3)/TapClose 吸收和全部诊断埋点；`UIManager:show(tv)` 后检测 `tv._bookloadcover_suppressed`，恢复 `tv.paintTo`（从 `_original_paintTo_bookloadcover`）/`visible`/`hasVisibleContent` 并 `setDirty` 强制重绘；未装该补丁时分支跳过（防御性）。未 push、未发版（依"发版须经同意"纪律）。
- **English**: v1.0.13. Stack-trace logging on KPW3 pinpointed the real culprit of the About popup flash/missing — the BookLoadCover Plus patch on the same device. Its `patchUIManagerShow` misclassifies any widget whose text contains both "关闭" and "书" as a "close book" notice and silently nulls its `paintTo`. Removed v1.0.12's TapClose-absorb hack and all diagnostics; after `UIManager:show(tv)`, if `tv._bookloadcover_suppressed`, restore `paintTo`/`visible`/`hasVisibleContent` and force repaint (skipped when the patch isn't installed). Not pushed / not released.

## 阶段二十八 / Phase 28 — 路线 C 真机日志锁定根因·初步根治关于弹窗（2026-08-11）
- **中文**：v1.0.12。在关于回调注入 `[AG_DEBUG]` 时序日志，KPW3 真机回传后锁定当时的修复方向：viewer 实测为 nil（排除"叠加覆盖"），弹窗被 TextViewer 自身的 TapClose 残留抬起手势秒关（存活仅 0.119s）。据此移除旧延迟、加 0.3s TapClose 吸收窗。⚠ 事后被阶段二十九推翻：该"残留 tap 误关"假设被 BookLoadCover Plus 误判真因掩盖，v1.0.12 实为失败修复（用户已立"发版须经同意"纪律，未再擅自发版）。
- **English**: v1.0.12. Injected `[AG_DEBUG]` timing logs; on-device data pointed to TapClose residual-gesture closing the popup (viewer was nil, ruling out overlay). Applied a 0.3s TapClose-absorb window. ⚠ Superseded by Phase 29: this hypothesis was masked by the real BookLoadCover Plus cause; v1.0.12 was an ineffective fix.

## 阶段二十七 / Phase 27 — 关于弹窗借鉴 poker24 加 show_menu=false（2026-08-11）
- **中文**：v1.0.11。用户要求参考 poker24.koplugin 的「游戏规则」说明弹窗（同为 TextViewer 大面积纯文本）与 artgallery 修复后的「关于 美术馆」弹窗作比对。实测对比两处源码：poker24 经由自建 ButtonDialog 菜单（modal）主动 close 后同步 show、下层 GameUI 为按钮驱动不持续刷新 → 无需延迟；artgallery 经由读者主菜单 TouchMenu（callback 后才 closeMenu + 关闭动画整屏重绘）、下层 ArtGalleryViewer 为非 modal 全屏图片查看器、交互会刷新自身 → 必须保留 closeMenu + scheduleIn(0.3) + modal + close_callback（照搬 poker24 同步写法会退回 v1.0.8「无弹窗」）。两者共同精髓（先关菜单 + modal）artgallery 已吸收。唯一值得借鉴 poker24 的是 `show_menu = false`：关于说明为纯只读文档，顶部 ⋯ 菜单（复制/搜索）多余，且其注释指出 TextViewer 内部菜单若 modal 处理不当会被遮蔽。已在关于回调的 TextViewer 中加 `show_menu = false`（main.lua:5898 附近），延迟与 close_callback 不变。三项校验全过。
- **English**: v1.0.11. Per user request, compared poker24.koplugin's "game rules" popup (also a large TextViewer) with artgallery's fixed "About" popup. Source review: poker24 shows its TextViewer synchronously after actively closing its own modal ButtonDialog, and its underlying GameUI is button-driven (no continuous repaint) → no delay needed. artgallery shows the About popup via the reader's TouchMenu (which only closes after the callback, with a full-screen repaint animation) over a non-modal fullscreen image viewer that repaints on interaction → it must keep closeMenu + scheduleIn(0.3) + modal + close_callback (copying poker24's synchronous style would regress to v1.0.8's "no popup"). The shared essence (close-menu-first + modal) is already adopted. The one improvement worth borrowing is `show_menu = false`: the About text is read-only, so its top ⋯ menu (copy/search) is redundant and risks the internal menu being obscured. Added `show_menu = false` to the About TextViewer (main.lua:~5898); delay and close_callback unchanged. All three checks pass.

## 阶段二十六 / Phase 26 — 修复「关于弹窗被全屏 viewer 覆盖」与「放大图库收藏/忽略角标」（2026-08-11）
- **中文**：v1.0.10。修复 KPW3 实机复测反馈的 2 项。①「关于 美术馆」仍无弹窗（但新增白色矩形闪烁）——阶段二十五的「菜单关闭竞态」修复不完整：本项是在「viewer 已全屏显示」之上叠加 TextViewer，viewer 作为全屏组件仍会在其后刷新周期把说明覆盖掉（TextViewer 的 onShow 仅标脏一次）→ 白闪即消失。改法：给关于回调的 TextViewer 加 `modal = true`（KOReader 中 modal 独占输入，使 viewer 在说明显示期间收不到点击、不再刷新覆盖），关闭后由 `close_callback` 恢复 viewer 刷新；保留 closeMenu + scheduleIn(0.3)。② 图库（全部）缩略图右下角收藏 ⭐ / 忽略（划掉眼睛）角标尺寸由 `csize = scaleBySize(18)` 放大为 `scaleBySize(36)`（即两倍，同时用于 SVG 渲染分辨率，放大后仍清晰）。三项校验全过。
- **English**: v1.0.10. Fixed 2 real-device items. ① The "About" popup still didn't appear (with a new white-rectangle flash) — Phase 25's menu-close fix was incomplete: here TextViewer is overlaid on top of an already-fullscreen viewer, which keeps repainting over the popup (TextViewer's onShow only dirties itself once) → the flash vanishes. Fix: add `modal = true` to the About TextViewer (modal captures all input in KOReader, so the viewer underneath receives no taps and stops repainting over it); a `close_callback` restores the viewer's refresh on close; keep closeMenu + scheduleIn(0.3). ② The gallery (All) thumbnail corner badges — favorite ⭐ / ignored (crossed-out eye) — were enlarged from `scaleBySize(18)` to `scaleBySize(36)` (2×; also the SVG render resolution, so still crisp). All three checks pass.

## 阶段二十五 / Phase 25 — 修复「全屏图库忽略后退半屏」与「关于弹窗仍不显示」（2026-08-10）
- **中文**：v1.0.9。修复 KPW3 实机复测反馈的 2 个 bug。① 全屏图库内忽略 / 取消忽略后退半屏——`on_ignore`/`on_unignore` 关闭 viewer 后 `showViewer` 重开，新 viewer 默认抽屉态，且 `_toggleFullscreen` 在画廊态直接 return，无法恢复全屏；现于 `_pending_gallery` 传入 `fullscreen` 标志，在 `_enterGallery` 前直接置位 `_fullscreen` 与 `panel_ratio`（1.0=铺满）维持全屏。②「关于 美术馆」仍无弹窗——真实根因为菜单关闭竞态：回调同步 `show(TextViewer)` 后 `onMenuSelect` 立即 `closeMenu()`，菜单关闭重绘盖住弹窗；照搬本插件「打开美术馆」已验证模式，先 `closeMenu()` 再 `scheduleIn(0.3, 显示)`，消除竞态（阶段二十四曾误判「弹窗逻辑正确」）。上半屏闪烁系弹窗失败后乱点命中顶区唤起菜单所致，弹窗正常后消失。三项校验全过。
- **English**: v1.0.9. Fixed 2 real-device bugs. ① From a fullscreen gallery, ignore/un-ignore dropped back to the half-screen (drawer) gallery — `on_ignore`/`on_unignore` closed the viewer and `showViewer` reopened it in drawer mode, and `_toggleFullscreen` returns early in gallery mode; now the `fullscreen` flag is threaded through `_pending_gallery` and applied (set `_fullscreen` + `panel_ratio=1.0`) before `_enterGallery`. ② The "About" popup still didn't appear — the real cause was a menu-close race: the callback's synchronous `show(TextViewer)` was immediately covered by `onMenuSelect`'s `closeMenu()` repaint; mirroring this plugin's proven "Open ArtGallery" pattern (close menu first, then `scheduleIn(0.3, show)`) fixes it. The upper-screen flicker was a side effect of tapping around after the failed popup. All three checks pass.

---

## 阶段二十四 / Phase 24 — 修复四项真机 bug（收藏写入 / 忽略同步 / 图库黑底 / 关于与闪烁）（2026-08-10）
- **中文**：v1.0.8。修复 KPW3 实机复测反馈的 4 个 bug：① 收藏「无法写入收藏」——`addFavorite` 仅建末级目录、父目录 `artgallery` 缺失致 `lfs.mkdir` 静默失败，新增递归建目录 `_mkdirs` 解决；② 全屏忽略未同步到「图库（忽略）」——`_hideCurrentImage` 未迁移 `ignored_metas/ignored_list` 内存池，已镜像图库态分区迁移并重置 `_derived_ok`；③ 全屏进图库整屏变黑——背景判定加 `not self._gallery_mode` 使画廊恒白底；④ 关于弹窗与上半屏闪烁——移除关于项冗余 `help_text`，并门控沉浸态（`_chrome_hidden`）顶部菜单唤起以消除闪烁。三项校验（语法/类顺序/加载）全过。
- **English**: v1.0.8. Fixed 4 real-device bugs: ① "Cannot write favorite" — `addFavorite` only created the leaf dir and the parent `artgallery` was missing, silently failing `lfs.mkdir`; added a recursive `_mkdirs`. ② Fullscreen-ignore not appearing in the "Ignored" gallery tab — `_hideCurrentImage` didn't migrate the `ignored_metas/ignored_list` pools; now mirrors the gallery-state partition move and resets `_derived_ok`. ③ Black gallery background when entering from fullscreen — background now forced white in gallery mode. ④ About popup + upper-screen flicker — removed the redundant `help_text` and gated the immersive-state top-menu trigger to stop flicker. All three checks (syntax / class-order / load) pass.

## 阶段二十 / Phase 20 — 内置更新器指向本仓库 + 关于/作者更正（2026-08-10）
- **中文**：内置更新器（移植自 Footcream）机制完整，唯一缺陷是 `github_repo` 仍指向旧仓库名 `ksaMask/mirador`，已改为真实仓库 `ksaMask123/artgallery.koplugin`，更新器现指向本仓库且确实可用（已实时核验 release API 可达）。「关于 美术馆」对话框插件名更正为「美术馆 / ArtGallery」、作者更正为 ksaMask123、更新链接同步更正；`_meta.lua` 同步（fullname/author/version 升 1.0.1）。为可真实验证更新，发布 v1.0.1 release 并上传 zip，设备内「检查更新」即可安装。代码改动已推送本仓库。
- **English**: The built-in updater (ported from Footcream) was complete; its only defect was `github_repo` still pointing to the old `ksaMask/mirador`, now changed to the real repo `ksaMask123/artgallery.koplugin` (release API reachability verified live). The "About" dialog name is corrected to "美术馆 / ArtGallery", author to ksaMask123, and the update link synced; `_meta.lua` updated (fullname/author/version→1.0.1). To make the updater genuinely testable, a v1.0.1 release with the zip was published so "Check for updates" can install it. Code changes pushed to the repo.

## 阶段十九 / Phase 19 — README 致谢溯源（2026-08-10）
- **中文**：在 README 中补全上游作者与项目链接致谢——Glimpse（作者 Fank1 / Erik Fanki）、Illustrations（作者 agaragou），中英双语对照。
- **English**: Added bilingual credits to README for upstream authors and project links — Glimpse (Fank1 / Erik Fanki) and Illustrations (agaragou).

## 阶段十八 / Phase 18 — 创建 GitHub 仓库并发布 v1.0.0（2026-08-10）
- **中文**：在 GitHub 新建公开仓库 `ksaMask123/artgallery.koplugin`，上传插件本体，配中英双语 README.md 与 CHANGELOG.md，并发布 v1.0.0 release（含打包 zip 供直接下载安装）。
- **English**: Created the public GitHub repo `ksaMask123/artgallery.koplugin`, uploaded the plugin with bilingual README.md and CHANGELOG.md, and published v1.0.0 with a downloadable zip.

## 阶段十七 / Phase 17 — 代码缺陷审查（2026-08-10）
- **中文**：聚焦阶段十六的拉伸缩放手势，逐段核查。修复一处真实缺陷：拉伸/铺满分支未门控 `_fullscreen`，会导致退出全屏后抽屉小图被非均匀拉伸变形；已加 `self._fullscreen and` 门控，守住"抽屉态恒为 contain"契约。并更正一处过时注释。其余（nil 兜底、手势路由、沉浸式取消链路、缩放回弹不变量）均健康。
- **English**: Reviewed the Phase-16 stretch zoom gestures line by line. Fixed a real defect: the stretch/cover fit branches were not gated by `_fullscreen`, which distorted the drawer thumbnail after exiting fullscreen; added a `self._fullscreen and` gate to honor the "drawer is always contain" contract. Also corrected a stale comment. The rest (nil fallbacks, gesture routing, immersive cancel chain, zoom-rebound invariants) were healthy.

## 阶段十六 / Phase 16 — 拉伸模式启用缩放手势（2026-08-10）
- **中文**：放开拉伸态的缩放手势——双击放大、双指张开放大、双指捏合缩小、按住图片移动全部可用。核心改动：删除两道缩放守卫、收窄拉伸渲染分支（`scale_factor==0` 才非均匀拉伸）、新增 `onZoomIn/onZoomOut` 修复"从填满态进不了缩放"的数学坑；缩放回弹到下限即归 0 回到拉伸填满。
- **English**: Enabled zoom gestures in stretch mode — double-tap zoom, pinch/spread zoom, and hold-to-pan all work. Key changes: removed two zoom guards, narrowed the stretch render branch (non-uniform stretch only at `scale_factor==0`), and added `onZoomIn/onZoomOut` overrides to fix the "cannot zoom out of fill state" math pitfall; zoom rebounding to the lower bound returns to 0 (stretch fill).

## 阶段十五（修订）/ Phase 15 (rev) — 布局简化：三态收进「右 ⋯」菜单（2026-08-10）
- **中文**：用户审阅后指出底部居中三态按钮簇多余，撤裁导航栏底部按钮簇，铺满/适配/拉伸三个选项统一收进「右 ⋯」菜单（单击切换、长按设默认）。
- **English**: After review, the bottom-center three-button cluster was deemed redundant; removed it and consolidated cover/contain/stretch into the `⋯` menu (tap to switch, long-press to set default).

## 阶段十五 / Phase 15 — 全屏「拉伸」模式（三态）+ 长按设默认（2026-08-10）
- **中文**：新增全屏三态填充（铺满/适配/拉伸）。拉伸态按屏幕长宽非均匀拉伸刚好填满整屏，不裁切、无黑边；默认 cover，顺序铺满→适配→拉伸；首次选拉伸弹一次性变形确认；「右 ⋯」菜单与导航栏均提供三态选项，长按可设为默认全屏看图模式。
- **English**: Added three fullscreen fit modes (cover/contain/stretch). Stretch non-uniformly scales the image to exactly fill the screen (no crop, no letterbox); default is cover, order cover→contain→stretch; first-time stretch shows a one-time distortion confirmation; both the `⋯` menu and the navbar expose the three modes, long-press sets the default fullscreen mode.

## 阶段十四 / Phase 14 — 菜单「关于 ArtGallery」汉化（2026-08-10）
- **中文**：统一插件对外中文名为「美术馆」，将菜单中残留的"关于 ArtGallery"与帮助文本一并汉化。
- **English**: Unified the public Chinese name as 美术馆 and localized the leftover "关于 ArtGallery" menu item and its help text.

## 阶段十三 / Phase 13 — 全屏沉浸式阅读（2026-08-10）
- **中文**：进入全屏即隐藏功能按钮 / 页码圆点 / 左上标题；点屏幕底部恢复，点图面延迟 0.35s 再隐藏（双击缩放不被打断）；补 `_pill_frame` 释放防泄漏。附 KPW3 内存/续航审查（内存低且受控、续航几乎无影响）。
- **English**: Entering fullscreen hides buttons / page dots / caption; tap the bottom edge to restore, tap the image to auto-hide after 0.35s (double-tap zoom unaffected); added `_pill_frame` free to prevent leaks. Includes a KPW3 memory/battery review (low, controlled memory; negligible battery impact).

## 阶段十二 / Phase 12 — EPUB 漫画看图同步进度（独立开关）（2026-08-10）
- **中文**：补齐滚动文档 EPUB 的看图进度同步，与分页文档各用独立开关、各自"只前进不倒退"。修复滚动文档下 `self.ui.paging` 非 nil 导致的误判（须先判 `self.ui.rolling`）。
- **English**: Added reading-progress sync for scrolling EPUB comics, with a separate toggle from paged documents, each advancing-only. Fixed the misclassification where `self.ui.paging` is non-nil under scrolling docs (must check `self.ui.rolling` first).

## 阶段十一 / Phase 11 — 看图同步书籍阅读进度（分页文档）（2026-08-10）
- **中文**：通过美术馆看图时同步更新书籍本身进度（CBZ/DjVu/PDF 等分页文档），避免看完漫画书本进度仍停第一页。
- **English**: Viewing images via ArtGallery now also advances the book's own progress for paged documents (CBZ/DjVu/PDF), so finishing a comic no longer leaves the book on page one.

## 阶段十 / Phase 10 — .MOBI 漫画支持方案（仅设计，搁置）（2026-08-09）
- **中文**：产出 .MOBI 漫画支持的纯设计方案，未落地实现，按用户要求搁置待议。
- **English**: Produced a design-only plan for .MOBI comic support; not implemented, deferred per user request.

## 阶段九 / Phase 9 — 代码审查与健壮性加固（2026-08-09）
- **中文**：用户要求查缺陷并加固，提升错误处理与边界健壮性。
- **English**: User-requested defect review and hardening of error handling and edge cases.

## 阶段八 / Phase 8 — 全屏渲染缺陷修复（2026-08-09）
- **中文**：修复全屏下右列露书页、图片只露左半两类渲染缺陷。
- **English**: Fixed two fullscreen render defects: book page bleeding on the right column, and image showing only its left half.

## 阶段七 / Phase 7 — 智能适配屏幕（2026-08-09）
- **中文**：自动选向（依图片宽高比旋转以最佳填满）+ 全屏铺满/适配切换。
- **English**: Auto-orientation (rotate by aspect ratio for best fill) plus fullscreen cover/contain toggle.

## 阶段六 / Phase 6 — 四项体验修复（2026-08-09）
- **中文**：菜单中文名、全书搜索记忆、按书清缓存、全屏铺满。
- **English**: Menu Chinese name, persistent in-book search memory, per-book cache clearing, fullscreen cover.

## 阶段五 / Phase 5 — 更名（Mirador → 美术馆）（2026-08-09）
- **中文**：插件对外名称统一为「美术馆 / ArtGallery」。
- **English**: Unified the public plugin name as 美术馆 / ArtGallery.

## 阶段四 / Phase 4 — 全面审查（2026-08-09）
- **中文**：对照 KOReader 纯洁源码 `koreader-kindle-v2026.07.1` 做全面审查。
- **English**: Full review against the pristine KOReader source `koreader-kindle-v2026.07.1`.

## 阶段三 / Phase 3 — 统一抽取/缓存层 + 收尾自检（2026-08-09）
- **中文**：统一图片抽取与缓存层，迁移旧收藏，收尾自检；v1.0.0 功能闭环。
- **English**: Unified the image extraction/cache layer, migrated legacy favorites, final self-check; v1.0.0 feature-complete.

## 阶段二 / Phase 2 — 移植 Illustrations 增强（2026-08-09）
- **中文**：收藏、更新+关于（继承）、最小尺寸（继承）、CBZ、三选一作用域，五项增强落地。
- **English**: Ported five enhancements: favorites, update+about (inherited), min-size (inherited), CBZ support, and three-way scope.

## 阶段一 / Phase 1 — 底座搭建 + 改名 + 全屏切换（2026-08-09）
- **中文**：搭建插件底座（基于 Glimpse 合并 Illustrations），完成改名 Mirador→美术馆，实现抽屉预览与一键全屏切换。
- **English**: Laid the plugin foundation (Glimpse + Illustrations merge), renamed Mirador→美术馆, and implemented drawer preview with one-tap fullscreen.

---

### 备注 / Notes
- 版本 / Version: `1.0.19`（见 `_meta.lua`）。
- 详细改动、风险表与方案文档见 `audit/` 目录（中文）。
  Detailed changes, risk tables, and design docs are in the `audit/` folder (Chinese).
